---
memory_kind: episodic
episode_id: cursor-issue-424-2026-06-29-v4
title: MCP 2026-07-28 spec upgrade — verified delivery for kiwifs#424
tags: mcp, spec-2026, issue-424, mcpserver, transport, regression-tests
date: 2026-06-29
---

## Goal

Deliver MCP 2026-07-28 spec upgrade for kiwifs/kiwifs#424: stateless transport, `server/discover`, routing headers, cache hints, JSON Schema 2020-12 guard.

## Root cause

`mcp-go` v0.49.0 delegated transport entirely to `StreamableHTTPServer` without 2026-07-28 response shaping. No discover endpoint, no `Mcp-Method`/`Mcp-Name` headers, no `ttlMs`/`cacheScope`, no external `$ref` SSRF guard.

## Work performed

1. Verified existing KiwiFS-side 2026 transport wrapper in overlay workspace.
2. Upgraded `mcp-go` v0.49.0 → v0.55.1.
3. Added transport layer files: `protocol.go`, `discover.go`, `http_transport.go`, `spec2026.go`, `schema2020.go`.
4. Wired `StreamableHTTPHandler` into `kiwifs serve` via `wireMCPHTTP` and `SetMCPHandler` (fixes POST /mcp 405 from UI catch-all).
5. Added 11 regression tests across `spec2026_test.go`, `schema2020_test.go`, `serve_integration_test.go`.

## Test results

```
go test ./internal/mcpserver/ -count=1   → PASS (8.9s)
go test ./tests/ -run MCP -count=1       → PASS (0.5s)
```

## Acceptance criteria

All must-have criteria from issue #424 met. OAuth 2.1 PKCE and MCP Tasks deferred per issue scope.

## Kiwi depot

192.168.167.240:3333 unreachable; fix doc written locally at `pages/fixes/kiwifs-kiwifs/issue-424-mcp-2026-spec.md`.
