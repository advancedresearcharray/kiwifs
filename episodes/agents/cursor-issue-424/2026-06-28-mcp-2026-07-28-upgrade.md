---
memory_kind: episodic
episode_id: cursor-issue-424-2026-06-28
title: "Issue #424 — MCP 2026-07-28 spec upgrade"
tags: [kiwifs, mcp, issue-424, fleet]
date: 2026-06-28
---

# Issue #424 — MCP 2026-07-28 spec upgrade

## Task

Implement MCP 2026-07-28 breaking changes for KiwiFS Streamable HTTP transport: stateless (already partial), `server/discover`, routing headers, list caching metadata, backward compatibility.

## Approach

1. Searched Kiwi — no prior fix doc for issue 424 (gateway unavailable; checked repo history).
2. Confirmed `mcp-go` v0.55.1 still at `LATEST_PROTOCOL_VERSION = 2025-11-25`.
3. Implemented `wrapMCP2026` HTTP middleware in `mcp2026.go` rather than waiting for upstream.
4. Tests run on full repo clone (`/tmp/kiwifs-test`) — overlay workspace has read-only `internal/pipeline` preventing full module compile.

## Result

All 10 new regression tests pass. Existing `internal/api` MCP handler tests pass. PR opened against main.

## Files

- `internal/mcpserver/mcp2026.go` (new)
- `internal/mcpserver/mcp2026_test.go` (new)
- `internal/mcpserver/mcpserver.go` (StreamableHTTPHandler wire-up)
